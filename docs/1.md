# 01.了解结构

制作操作系统的第一步就是学会怎么使用汇编语言，今天咱们就学习一下

## 01.1 FAT12结构

FAT12文件系统是MsDos所使用的文件系统

并且还有一套文件分配表，文件分配表记录了文件在磁盘上的位置

还有一套"压缩"方式，当然，这个有个坑的点是压缩跟没压缩是一样的，甚至从简单的表变成了更复杂的表

(于是那缺德微软就脑子短路没有选择跳过 FAT12 直接发明 FAT16？)

请看下面的表

| FAT项| 硬盘中的表示|
| --- | --- |
| FF0 FFF | F0 FF FF|
| abc def  | bc fa de |

这样就搞得很恶心，FAT12 要考虑的细节有一半都来自这个破算法。比如，由于每两个 FAT 项占三个字节，所以极端情况下会出现某个 FAT 项的低八位在扇区 a，高四位在扇区 a+1，所以读取磁盘时，一次要读两个扇区，读到之后还得费尽心思转换。

## 01.2 BIOS中断码
BIOS中断码是计算机启动时，BIOS会加载到内存的0x0000:0x7c00处，然后执行0x0000:0x7c00处的代码，这个代码就是计算机启动时执行的代码，也就是引导扇区。

BIOS中断码是计算机启动时，BIOS会加载到内存的0x0000:0x7c00处，然后执行0x0000:0x7c00处的代码，这个代码就是计算机启动时执行的代码，也就是引导扇区。

当然，中断码是这样执行的

```asm
mov ah, 0x0E
mov al, 'A'
int 0x10
```

简单来说就是这样，中断码有0x10、0x13等中断码

执行只需要`int <中断码>`即可

## 01.3 了解寄存器

咱们先认识一下通用寄存器

分别为:`AX`、`BX`、`CX`、`DX`

然后是：`AH`、`BH`、`CH`、`DH`

然后是：`AL`、`BL`、`CL`、`DL`

然后是：`ES`、`CS`、`DS`、`SS`

然后是：`IP`、`SP`、`BP`、`SI`、`DI`

然后是：`EAX`、`EBX`、`ECX`、`EDX`

然后是：`EAX`、`EBX`、`ECX`、`EDX`

然后是：`EAX`、`EBX`、`ECX`、`EDX`

就这些了，每个寄存器都有不同的作用

OK，这些足够了，关于汇编语言......当你已经学会了吧(笑)
